pub struct TxReceiver;

impl TxReceiver {
    pub fn run() {
        println!("TODO:: Tx Receiver")
    }
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
